package torsdagsOpgave6;
/*
import java.util.Scanner;

public class Task2 {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.println("Indtast dit fødselsår:");

// Variabler
        boolean number = false;
        // Erklæringer vi bruger senere
        int year;
        int current;

        while (!number) { //Sørger for, at main bliver ved med at kører indtil boolean er true
            String input = scanner.nextLine();
            try {
                int tal = Integer.parseInt(input);
                current = 2025; // Det år vi er i
                year=tal; // Det tal som personen sætter ind
                if (year >= 1930 && year <= current){
                    number = true; //Hvis input er inden for if-statementet bliver boolean true
                } else { // hvis den ikke er, vil vi have en ny input
                    System.out.println("Type a vaild year");
                }
            } catch (NumberFormatException e) { //Her catcher vi, hvis de fx skriver et bogstav.
                System.out.println("Please only type numbers");
            }
        }
    }
}

 */